#!/usr/bin/env python3
import sys

sys.path.insert(0, "src")
from orange import Path
from orange.xlwt import Book, Sheet

with Book("~/table3.xlsx") as book:
    """
    book.add_table(
        pos="A1",
        columns=[
            Header("姓名", width=12, format="normal"),
            Header("年龄", width=15, format="percent"),
        ],
        data=[["张三", 0.23], ["历史", 0.34]],
    )
    """
    sheet = book.add_sheet("hello")
    sheet.set_widths(
        {
            "A:B": 20,
            "C:D,G": 17,
        }
    )
    sheet.set_columns("A:B", cell_format="Percent")
    sheet.set_hidden("D:E,I")

    print("导出文件成功")
